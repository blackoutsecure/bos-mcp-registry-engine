{
  "name": "mcp-registry-engine",
  "version": "1.0.0",
  "description": "Static MCP Registry Engine - Generates and validates MCP server registry",
  "type": "module",
  "main": "scripts/generate-registry.js",
  "scripts": {
    "generate": "node scripts/generate-registry.js",
    "validate": "node scripts/generate-registry.js --validate-only",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "registry",
    "model-context-protocol"
  ],
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "fs-extra": "^11.2.0",
    "semver": "^7.5.4"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  }
}
